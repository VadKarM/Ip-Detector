# Ip Detector

Программа получает ваш IP, определяет геолокацию и сохраняет данные на Яндекс.Диск.

## Установка

1. Клонируйте репозиторий
2. Создайте виртуальное окружение: `python -m venv venv`
3. Активируйте его: `source venv/bin/activate` (или `venv\Scripts\activate` на Windows)
4. Установите зависимости: `pip install -r requirements.txt`

## Настройка токена Яндекс.Диска

1. Получите OAuth-токен на [Полигоне Яндекса](https://yandex.ru/dev/disk/poligon/)
2. Создайте файл `.env` в корне проекта и добавьте: